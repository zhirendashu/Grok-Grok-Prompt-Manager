# 🚀 Twitter-X 智能助手 v3.0 - 快速安装指南

## ✅ 测试版本已准备好！

文件名：**【完整测试版】Twitter-X 智能助手 v3.0.user.js**
文件大小：~32KB
状态：✅ 可直接安装

---

## 📦 安装步骤

### 1. 安装 Tampermonkey
如果还没有安装，请先安装 Tampermonkey 浏览器扩展：
- Chrome: https://chrome.google.com/webstore
- Firefox: https://addons.mozilla.org
- Edge: https://microsoftedge.microsoft.com/addons

### 2. 安装脚本

**方法一：直接拖拽（最简单）**
1. 找到文件 `【完整测试版】Twitter-X 智能助手 v3.0.user.js`
2. 直接拖拽到浏览器窗口
3. Tampermonkey 会自动打开安装页面
4. 点击"安装"按钮

**方法二：手动安装**
1. 打开 Tampermonkey 管理面板
2. 点击 "+" 创建新脚本
3. 打开 `【完整测试版】Twitter-X 智能助手 v3.0.user.js`
4. 全选并复制内容（Ctrl+A, Ctrl+C）
5. 粘贴到 Tampermonkey 编辑器（Ctrl+V）
6. 保存（Ctrl+S）

### 3. 验证安装

1. 访问 https://x.com
2. 按 F12 打开控制台
3. 应该看到以下输出：

```
🧪 Twitter/X 智能助手 v3.0 测试版
由植人大树精心打造
个人主页: https://link3.cc/zhirendashu
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📖 使用指南:
  1. 启用自动点赞: TwitterAssistant.state.autoLike = true
  2. 启动自动化: TwitterAssistant.automation.start()
  3. 查看统计: TwitterAssistant.getInfo()
  4. 停止自动化: TwitterAssistant.automation.stop()
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[TWX] 模块初始化完成
[TWX] 所有功能模块已加载
[TWX] 高清媒体已启用
✅ 模块已自动初始化
💡 提示: 打开控制台输入 TwitterAssistant 查看可用方法
```

---

## 🎮 使用方法

### 方式一：右键菜单（推荐）

点击浏览器工具栏的 Tampermonkey 图标，会看到：
- 🚀 **启动自动化** - 一键启动自动点赞
- ⏸️ **停止自动化** - 停止所有自动化操作
- 📊 **查看统计** - 查看点赞/关注/转发统计
- 🔄 **重置统计** - 清空所有统计数据

### 方式二：控制台命令

在浏览器控制台（F12）中输入：

```javascript
// 1. 启用自动点赞
TwitterAssistant.state.autoLike = true;
TwitterAssistant.automation.start();

// 2. 查看统计
TwitterAssistant.getInfo();
// 输出: { name: '...', stats: { like: 10, ... } }

// 3. 停止自动化
TwitterAssistant.automation.stop();

// 4. 启用其他功能
TwitterAssistant.state.autoFollow = true;   // 自动关注
TwitterAssistant.state.autoRetweet = true;  // 自动转发
TwitterAssistant.state.originalsOnly = true; // 只看原创

// 5. 调整速率限制
TwitterAssistant.state.rate.like = 10;  // 每小时最多点赞 10 次

// 6. 查看日志
TwitterAssistant.logger.getLogs();

// 7. 重置统计
TwitterAssistant.state.resetStats();
```

---

## 🧪 测试功能

### 1. 测试高清图片 ✅
- 自动启用，无需手动操作
- 刷新页面，所有图片会自动变为 4096x4096 高清版本

### 2. 测试自动点赞 ✅
```javascript
TwitterAssistant.state.autoLike = true;
TwitterAssistant.automation.start();
// 观察控制台，应该看到 "[TWX] 已点赞" 的消息
```

### 3. 测试速率限制 ✅
```javascript
// 设置较低的限制以便测试
TwitterAssistant.state.rate.like = 5;
TwitterAssistant.automation.start();
// 应该在 5 次后停止点赞
```

### 4. 测试只看原创 ✅
```javascript
TwitterAssistant.state.originalsOnly = true;
TwitterAssistant.automation.start();
// 应该跳过所有转发的推文
```

---

## 📊 查看状态

### 实时统计
```javascript
const info = TwitterAssistant.getInfo();
console.log('点赞数:', info.stats.like);
console.log('关注数:', info.stats.follow);
console.log('转发数:', info.stats.retweet);
console.log('运行时间:', info.uptime, 'ms');
```

### 查看日志
```javascript
const logs = TwitterAssistant.logger.getLogs();
console.table(logs);
```

### 查看缓存
```javascript
console.log('视频缓存:', TwitterAssistant.state.videoCache.size);
console.log('图片缓存:', TwitterAssistant.state.imageCache.size);
```

---

## ⚠️ 重要提示

### 安全建议
1. ✅ 建议在测试账号上测试
2. ⚠️ 不要长时间运行自动化
3. ⚠️ 不要设置过高的频率
4. ⚠️ 避免触发 X.com 的反垃圾机制

### 默认配置
- 点赞限制：50次/小时
- 关注限制：20次/小时
- 转发限制：15次/小时
- 检查间隔：15秒
- 操作间隔：2.5秒

### 建议配置（测试用）
```javascript
// 降低限制以便测试
TwitterAssistant.state.rate.like = 5;
TwitterAssistant.state.rate.follow = 3;
TwitterAssistant.state.rate.retweet = 2;

// 增加间隔
TwitterAssistant.state.config.tickMs = 30000;  // 30秒检查一次
TwitterAssistant.state.config.actionInterval = 5000;  // 5秒操作间隔
```

---

## 🐛 常见问题

### Q1: 脚本没有自动运行
**A**: 检查 Tampermonkey 是否启用，刷新页面

### Q2: 控制台没有输出
**A**: 检查是否在 x.com 页面，检查脚本是否安装成功

### Q3: 自动化不工作
**A**: 确保已启用功能开关并调用 start() 方法

### Q4: 高清图片不生效
**A**: 刷新页面，检查控制台是否有 "高清媒体已启用" 的消息

---

## 📞 反馈

测试过程中如有问题，请记录：
1. 浏览器版本
2. 控制台完整错误信息
3. 复现步骤
4. 预期行为 vs 实际行为

---

## 📁 相关文件

- `【完整测试版】Twitter-X 智能助手 v3.0.user.js` - 可安装的完整版本
- `Twitter-X 智能助手模块 v3.0.js` - 核心模块代码
- `README.md` - 项目说明
- `测试指南.md` - 详细测试指南
- `如何创建测试版.md` - 组装说明

---

**祝测试顺利！** 🎉

有任何问题随时反馈！
