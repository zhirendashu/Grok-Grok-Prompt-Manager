# 🔌 Twitter/X 智能助手模块 - 集成指南

**版本**: v3.0.0
**作者**: 植人大树
**日期**: 2026-01-19

---

## 📋 模块概述

Twitter/X 智能助手模块设计为 **Grok Prompt Manager** 的扩展模块，支持两种运行模式：

1. **独立模式**: 作为单独的油猴脚本运行
2. **集成模式**: 作为 Grok Prompt Manager 的子模块运行

---

## 🏗️ 架构设计

### 模块结构
```
TwitterAssistant (命名空间)
├── version: '3.0.0'
├── name: 'Twitter/X 智能助手'
├── enabled: false
├── state: TwitterState 实例
├── logger: Logger 实例
├── integration: 集成接口
│   ├── mainScript: 主脚本引用
│   ├── stateManager: 共享状态管理器
│   ├── uiComponents: 共享 UI 组件
│   └── utils: 共享工具函数
└── methods:
    ├── init(options)
    ├── destroy()
    └── getInfo()
```

---

## 🚀 集成到主脚本

### 方法一：直接嵌入代码

在 `Grok_Prompt_Manager_Beta_Art.user.js` 中添加：

```javascript
// ========================================
// Twitter/X 智能助手模块
// ========================================

// 1. 在文件开头定义全局命名空间
const GPM = {
    version: '5.0.0',
    modules: {},

    // 注册模块
    registerModule(name, module) {
        this.modules[name] = module;
        console.log(`[GPM] 模块已注册: ${name}`);
    }
};

// 暴露到全局
window.GPM = GPM;

// 2. 在适当位置插入 Twitter 模块代码
// （将 Twitter-X 智能助手模块 v3.0.js 的内容复制到这里）

// 3. 在主脚本初始化时，检测并启动 Twitter 模块
function initGPM() {
    // ... 主脚本初始化代码 ...

    // 检测是否在 Twitter/X
    if (window.location.hostname.includes('twitter.com') ||
        window.location.hostname.includes('x.com')) {

        // 初始化 Twitter 模块
        if (GPM.modules.twitter) {
            GPM.modules.twitter.init({
                integration: {
                    mainScript: GPM,
                    stateManager: storage,  // 共享存储
                    uiComponents: {         // 共享 UI 组件
                        showToast: showToast,
                        createPanel: createPanel
                    },
                    utils: {                // 共享工具函数
                        sleep: sleep,
                        randomDelay: randomDelay
                    }
                }
            });
        }
    }
}
```

---

### 方法二：动态加载（推荐）

```javascript
// 在主脚本中添加模块加载器
const ModuleLoader = {
    modules: new Map(),

    // 注册模块
    register(name, moduleFactory) {
        this.modules.set(name, {
            factory: moduleFactory,
            instance: null,
            loaded: false
        });
    },

    // 加载模块
    async load(name, options = {}) {
        const module = this.modules.get(name);
        if (!module) {
            throw new Error(`模块不存在: ${name}`);
        }

        if (!module.loaded) {
            module.instance = module.factory(options);
            module.loaded = true;
        }

        return module.instance;
    },

    // 卸载模块
    unload(name) {
        const module = this.modules.get(name);
        if (module && module.instance?.destroy) {
            module.instance.destroy();
            module.loaded = false;
            module.instance = null;
        }
    }
};

// 注册 Twitter 模块
ModuleLoader.register('twitter', (options) => {
    // 返回 TwitterAssistant 实例
    return window.TwitterAssistant;
});

// 在需要时加载
if (window.location.hostname.includes('twitter.com') ||
    window.location.hostname.includes('x.com')) {

    const twitterModule = await ModuleLoader.load('twitter', {
        integration: {
            mainScript: GPM,
            // ... 其他配置
        }
    });

    twitterModule.init();
}
```

---

## 📦 共享资源

### 1. 状态管理器
```javascript
// 主脚本的 Storage 类可以被 Twitter 模块复用
TwitterAssistant.init({
    integration: {
        stateManager: {
            save: (key, value) => GM_setValue(key, value),
            load: (key, defaultValue) => GM_getValue(key, defaultValue),
            remove: (key) => GM_deleteValue(key)
        }
    }
});
```

### 2. UI 组件
```javascript
// 共享主脚本的 Toast、Modal 等组件
TwitterAssistant.init({
    integration: {
        uiComponents: {
            showToast: (msg, type) => {
                // 使用主脚本的 Toast
                GPM.ui.showToast(msg, type);
            },
            createPanel: (config) => {
                // 使用主脚本的面板创建器
                return GPM.ui.createPanel(config);
            }
        }
    }
});
```

### 3. 工具函数
```javascript
// 共享工具函数，避免重复定义
TwitterAssistant.init({
    integration: {
        utils: {
            sleep: GPM.utils.sleep,
            randomDelay: GPM.utils.randomDelay,
            throttle: GPM.utils.throttle,
            debounce: GPM.utils.debounce
        }
    }
});
```

---

## 🎯 使用示例

### 独立模式
```javascript
// 作为单独的油猴脚本运行
// 自动初始化，无需额外配置
```

### 集成模式
```javascript
// 在主脚本中
const GPM = {
    version: '5.0.0',
    modules: {},
    storage: new Storage(),
    ui: new UIManager(),
    utils: Utils
};

window.GPM = GPM;

// Twitter 模块会自动检测 GPM 并注册
// GPM.modules.twitter 可用

// 初始化
GPM.modules.twitter.init({
    integration: {
        mainScript: GPM,
        stateManager: GPM.storage,
        uiComponents: GPM.ui,
        utils: GPM.utils
    }
});

// 获取模块信息
const info = GPM.modules.twitter.getInfo();
console.log(info);
// {
//     name: 'Twitter/X 智能助手',
//     version: '3.0.0',
//     enabled: true,
//     stats: { like: 0, follow: 0, retweet: 0, download: 0 },
//     uptime: 12345
// }

// 销毁模块
GPM.modules.twitter.destroy();
```

---

## 🔧 配置选项

### 初始化选项
```javascript
TwitterAssistant.init({
    // 集成配置
    integration: {
        // 主脚本引用
        mainScript: GPM,

        // 共享状态管理器
        stateManager: {
            save: (key, value) => {},
            load: (key, defaultValue) => {},
            remove: (key) => {}
        },

        // 共享 UI 组件
        uiComponents: {
            showToast: (msg, type) => {},
            createPanel: (config) => {},
            createModal: (config) => {}
        },

        // 共享工具函数
        utils: {
            sleep: (ms) => {},
            randomDelay: (min, max) => {},
            throttle: (fn, delay) => {},
            debounce: (fn, delay) => {}
        }
    },

    // 模块配置
    config: {
        autoStart: false,
        defaultTheme: 'dark',
        logLevel: 'info'
    }
});
```

---

## 📊 API 接口

### TwitterAssistant.init(options)
初始化模块

**参数**:
- `options.integration` - 集成配置
- `options.config` - 模块配置

**返回**: 无

---

### TwitterAssistant.destroy()
销毁模块，清理所有资源

**参数**: 无
**返回**: 无

---

### TwitterAssistant.getInfo()
获取模块信息

**参数**: 无
**返回**:
```javascript
{
    name: string,
    version: string,
    enabled: boolean,
    stats: object,
    uptime: number
}
```

---

## 🎨 UI 集成

### 在主脚本面板中添加 Twitter 控制

```javascript
// 在主脚本的 SidePanel 中添加 Twitter 标签
const twitterTab = document.createElement('div');
twitterTab.innerHTML = `
    <div class="twitter-controls">
        <h3>Twitter 助手</h3>
        <button id="twitter-toggle">启动/停止</button>
        <button id="twitter-settings">设置</button>
        <div id="twitter-stats"></div>
    </div>
`;

// 绑定事件
document.getElementById('twitter-toggle').onclick = () => {
    if (GPM.modules.twitter.state.running) {
        GPM.modules.twitter.stop();
    } else {
        GPM.modules.twitter.start();
    }
};
```

---

## 🔒 安全考虑

### 命名空间隔离
```javascript
// 所有代码都在 TwitterAssistant 命名空间下
// 避免污染全局作用域
TwitterAssistant.state.xxx
TwitterAssistant.ui.xxx
TwitterAssistant.automation.xxx
```

### 资源清理
```javascript
// 模块销毁时自动清理所有资源
TwitterAssistant.destroy();
// - 移除所有事件监听器
// - 断开所有观察器
// - 清空所有缓存
// - 移除所有 UI 元素
```

---

## 📝 开发计划

### 阶段一：核心架构 ✅
- [x] 模块化设计
- [x] 状态管理
- [x] 事件管理
- [x] 日志系统
- [x] 集成接口

### 阶段二：功能模块 ⏳
- [ ] UI 模块
- [ ] 自动化模块
- [ ] 下载模块
- [ ] 搜索模块

### 阶段三：集成测试 ⏳
- [ ] 独立模式测试
- [ ] 集成模式测试
- [ ] 性能测试
- [ ] 兼容性测试

---

## 💡 最佳实践

### 1. 延迟加载
```javascript
// 只在 Twitter/X 页面加载模块
if (window.location.hostname.includes('twitter.com') ||
    window.location.hostname.includes('x.com')) {
    // 加载 Twitter 模块
}
```

### 2. 按需初始化
```javascript
// 用户点击时才初始化
document.getElementById('enable-twitter').onclick = () => {
    if (!GPM.modules.twitter.enabled) {
        GPM.modules.twitter.init();
    }
};
```

### 3. 资源共享
```javascript
// 尽可能复用主脚本的资源
// 避免重复定义工具函数、UI 组件等
```

---

## 🐛 调试

### 启用调试模式
```javascript
TwitterAssistant.debug = true;
TwitterAssistant.init();
```

### 查看模块状态
```javascript
console.log(TwitterAssistant.getInfo());
console.log(TwitterAssistant.state);
console.log(TwitterAssistant.logger.getLogs());
```

---

**集成指南结束**
