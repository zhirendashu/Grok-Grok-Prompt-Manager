# 🧪 Twitter-X 智能助手 v3.0 - 测试指南

**日期**: 2026-01-19
**版本**: v3.0.0
**状态**: 可独立测试

---

## 📦 当前已完成的功能

### ✅ 核心模块
1. **LRU 缓存** - 防止内存泄漏
2. **事件管理器** - 统一管理监听器
3. **工具函数集** - 节流/防抖/DOM 查询
4. **状态管理** - 集中管理所有状态
5. **日志系统** - 限制日志数量

### ✅ 功能模块
1. **自动化模块** - 自动点赞/关注/转发
2. **媒体下载模块** - 高清图片 + 视频下载
3. **DOM 选择器** - 基于最新 X.com 结构

---

## 🚀 如何测试

### 方法一：直接在浏览器控制台测试

1. **打开 X.com**
2. **打开浏览器控制台** (F12)
3. **复制并执行模块代码**:

```javascript
// 1. 复制 Twitter-X 智能助手模块 v3.0.js 的全部内容
// 2. 粘贴到控制台并回车

// 3. 初始化模块
TwitterAssistant.init();

// 4. 测试自动化功能
TwitterAssistant.state.autoLike = true;  // 启用自动点赞
TwitterAssistant.automation.start();      // 启动自动化

// 5. 查看统计
console.log(TwitterAssistant.getInfo());

// 6. 停止自动化
TwitterAssistant.automation.stop();
```

---

### 方法二：作为油猴脚本安装

#### 步骤 1: 创建完整的 UserScript

创建文件 `Twitter-X-Assistant-v3.0-Test.user.js`:

```javascript
// ==UserScript==
// @name         Twitter/X 智能助手 v3.0 测试版
// @namespace    https://link3.cc/zhirendashu
// @version      3.0.0
// @description  测试版本 - 独立运行
// @author       植人大树
// @match        https://twitter.com/*
// @match        https://x.com/*
// @grant        GM_setValue
// @grant        GM_getValue
// @grant        GM_download
// @run-at       document-idle
// ==/UserScript==

(function() {
    'use strict';

    // 在这里粘贴 Twitter-X 智能助手模块 v3.0.js 的全部内容
    // ...

    // 自动初始化
    if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', () => {
            setTimeout(() => TwitterAssistant.init(), 1000);
        });
    } else {
        setTimeout(() => TwitterAssistant.init(), 1000);
    }
})();
```

#### 步骤 2: 安装到 Tampermonkey

1. 打开 Tampermonkey 管理面板
2. 点击 "+" 创建新脚本
3. 粘贴上面的代码
4. 保存
5. 刷新 X.com 页面

---

## 🎮 测试功能

### 1. 测试高清图片

```javascript
// 在控制台执行
TwitterAssistant.state.hdMedia = true;
TwitterAssistant.download.enableHDMedia();

// 刷新页面，观察图片是否变为高清
```

### 2. 测试自动点赞

```javascript
// 启用自动点赞
TwitterAssistant.state.autoLike = true;
TwitterAssistant.automation.start();

// 观察控制台日志
// 应该看到 "[TWX] 已点赞" 的消息

// 查看统计
console.log('点赞数:', TwitterAssistant.state.count.like);

// 停止
TwitterAssistant.automation.stop();
```

### 3. 测试速率限制

```javascript
// 设置较低的限制以便测试
TwitterAssistant.state.rate.like = 5;  // 只允许点赞 5 次

// 启动自动化
TwitterAssistant.automation.start();

// 观察是否在 5 次后停止点赞
```

### 4. 测试只看原创

```javascript
// 启用只看原创
TwitterAssistant.state.originalsOnly = true;
TwitterAssistant.automation.start();

// 应该跳过所有转发的推文
```

### 5. 测试媒体下载

```javascript
// 获取第一条包含图片的推文
const tweet = document.querySelector('article[data-testid="tweet"]');
const hasMedia = tweet.querySelector('div[data-testid="tweetPhoto"]');

if (hasMedia) {
    // 下载媒体
    TwitterAssistant.download.downloadMedia(tweet);
}
```

---

## 📊 查看状态

### 获取模块信息

```javascript
const info = TwitterAssistant.getInfo();
console.log('模块信息:', info);
// 输出:
// {
//     name: 'Twitter/X 智能助手',
//     version: '3.0.0',
//     enabled: true,
//     stats: { like: 10, follow: 5, retweet: 3, download: 0 },
//     uptime: 123456
// }
```

### 查看日志

```javascript
const logs = TwitterAssistant.logger.getLogs();
console.table(logs);
```

### 查看缓存状态

```javascript
console.log('视频缓存大小:', TwitterAssistant.state.videoCache.size);
console.log('图片缓存大小:', TwitterAssistant.state.imageCache.size);
```

---

## 🐛 调试技巧

### 1. 启用详细日志

```javascript
// 监听日志更新
TwitterAssistant.logger.onLogUpdate = (log) => {
    console.log(`[${log.timestamp}] [${log.type}] ${log.message}`);
};
```

### 2. 检查选择器是否正确

```javascript
// 测试选择器
const tweets = document.querySelectorAll('article[data-testid="tweet"]');
console.log('找到推文数量:', tweets.length);

const likeButtons = document.querySelectorAll('button[data-testid="like"]');
console.log('找到点赞按钮数量:', likeButtons.length);
```

### 3. 手动触发功能

```javascript
// 手动处理一次推文
TwitterAssistant.automation.processTweets();

// 手动提升图片质量
TwitterAssistant.download.enhanceMediaQuality();
```

---

## ⚠️ 注意事项

### 1. 速率限制
- 默认限制: 点赞 50次/小时, 关注 20次/小时, 转发 15次/小时
- 建议测试时降低限制，避免触发 X.com 的反垃圾机制

### 2. 自动化风险
- **不要长时间运行自动化**
- **不要设置过高的频率**
- **建议只在测试环境使用**

### 3. 性能监控
```javascript
// 监控性能
console.log('已处理推文数:', TwitterAssistant.state.processed.size);
console.log('事件监听器数:', TwitterAssistant.state.eventManager.listeners.length);
```

---

## 🔧 配置调整

### 修改自动化间隔

```javascript
// 默认 15 秒检查一次
TwitterAssistant.state.config.tickMs = 30000;  // 改为 30 秒

// 默认每次最多处理 2 条推文
TwitterAssistant.state.config.maxActionsPerCycle = 1;  // 改为 1 条

// 默认操作间隔 2.5 秒
TwitterAssistant.state.config.actionInterval = 5000;  // 改为 5 秒
```

### 导出/导入配置

```javascript
// 导出配置
const config = TwitterAssistant.state.exportConfig();
console.log(JSON.stringify(config, null, 2));

// 导入配置
const savedConfig = {
    version: '3.0.0',
    autoLike: true,
    autoFollow: false,
    // ...
};
TwitterAssistant.state.importConfig(savedConfig);
```

---

## 📝 测试清单

### 基础功能
- [ ] 模块初始化成功
- [ ] 日志系统正常
- [ ] 状态管理正常

### 自动化功能
- [ ] 自动点赞正常
- [ ] 自动关注正常
- [ ] 自动转发正常
- [ ] 速率限制生效
- [ ] 只看原创过滤生效

### 媒体功能
- [ ] 高清图片替换成功
- [ ] 图片下载成功
- [ ] 视频下载成功（如果实现）

### 性能
- [ ] 内存使用正常
- [ ] CPU 占用正常
- [ ] 无内存泄漏

---

## 🚨 常见问题

### Q1: 模块未初始化
**A**: 检查是否在 X.com 页面，检查控制台是否有错误

### Q2: 自动化不工作
**A**: 检查功能开关是否启用，检查速率限制是否已达上限

### Q3: 高清图片不生效
**A**: 刷新页面，检查 `hdMedia` 是否为 `true`

### Q4: 下载失败
**A**: 检查是否有 `GM_download` 权限，检查浏览器下载设置

---

## 📞 反馈

测试过程中如有问题，请记录：
1. 浏览器版本
2. 控制台错误信息
3. 复现步骤
4. 预期行为 vs 实际行为

---

**祝测试顺利！** 🎉
